/** Returns a lower case string with leading and trailing whitespace removed
 * 
 * @param {string} string 
 * @returns {string}
 */
export function clean(string) {
    return string.toLowerCase().trim();
}